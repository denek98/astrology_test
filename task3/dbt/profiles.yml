default:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: astrology_db
      schema: 'main'
      schemas_and_paths:
        main: '../task3.db'
      schema_directory: '/'
